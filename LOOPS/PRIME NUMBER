<!-- PRIME NUMBER PROGRAM -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loops</title>
</head>
<script>
    function main(){
        n=prompt("Enter number : ");
        let count=0;

        // APROACH 1
        // for(let i=1;i<=n;i++){
        //     if(n%i==0) count++;
        // }
        // if(count==2) console.log(`Given number ${n} is Prime Number`);
        // else console.log(`Given number ${n} is not Prime Number`);
        // }


        //APROACH 2
        // for(let i=2;i<n;i++){
        //     if(n%i==0) count++;
        // }
        // if(count==0) console.log(`Given number ${n} is Prime Number`);
        // else console.log(`Given number ${n} is not Prime Number`);
        //  }


        // APROACH 3
        for(let i=2;i<=n/2;i++){
            if(n%i==0) count++;
        }
        if(count==0) console.log(`Given number ${n} is Prime Number`);
        else console.log(`Given number ${n} is not Prime Number`);

        }

    
    main();
</script>
<body>
    <h1>Loops statements</h1>
    
</body>
</html>
